sonar.projectKey=olexmal_MegaBrain
sonar.organization=olexmal

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=MegaBrain
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# TypeScript configuration
sonar.typescript.lcov.reportPaths=coverage/lcov.info
sonar.testExecutionReportPaths=test-report.xml

# Exclusions for common frontend files
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/*.spec.ts,**/*.test.ts,**/*.d.ts

# Coverage exclusions (optional - adjust based on your setup)
sonar.coverage.exclusions=**/*.spec.ts,**/*.test.ts,**/main.ts,**/polyfills.ts,**/*.config.ts
